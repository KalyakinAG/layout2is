
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	_Обработка = РеквизитФормыВЗначение("Объект");
	СтраницаHTML = _Обработка.ПолучитьМакет("СтраницаHTML");
	ТекстHTML = СтраницаHTML.ПолучитьТекст();
КонецПроцедуры

&НаКлиенте
Процедура КомандаПрименить(Команда)
	Document = Элементы.ПолеHTML.Документ.defaultView;
	Document.findRefs();
	Ссылки = Document.result; 
	Закрыть(Ссылки);
КонецПроцедуры
